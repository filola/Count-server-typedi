config:
  target: http://localhost:3000
  tls:
  rejectUnauthorized: false
  phases:
    - duration: 200
      arrivalRate: 10
scenarios:
  - name: "파일을 생서 후, 삭제한다."
    flow:
      - post:
          url: "/disconnect"
